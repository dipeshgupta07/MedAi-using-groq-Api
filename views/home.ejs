<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Chatbox UI</title>
    <script src="https://cdn.tailwindcss.com"></script>
  
 <link rel="stylesheet" href="/css/style.css">

</head>
<body class="bg-gradient-to-r from-indigo-500 via-purple-500 to-pink-500 h-screen flex items-center justify-center">
    <div class="bg-white w-[900px] h-[700px] p-6 rounded-2xl shadow-2xl bigbox"  >
        <h1 class="text-2xl font-bold text-center mb-4">MedAi</h1>
        <div class="h-[600px] overflow-y-auto border rounded-lg p-3 bg-gray-50 space-y-2 mb-4"  id="chatBox">
          
                <div class="chatbox2">
                    
                        <% for (chat of chats){ %>
                            <% if (chat.sender == "user"){ %>
                                <div class="user">
                                    <b><h1><%= chat.message %></h1></b>

                                </div>

                            <% } %>
                            <% if(chat.sender == "bot"){ %>
                                <div class="bot">
                                    <div class="whitespace-pre-wrap font-semibold text-base leading-relaxed wido"><%= chat.message %></div>


                                </div>

                            <% } %>
                                

                            
                            


                        <% } %>
                       
                   
                </div>
           
        </div>
        <form action="/chat/<%= sessionId %>" method="POST" class="flex gap-2">



            <input id="message" name="message" type="text" placeholder="Type your message..." required
                   class="flex-grow px-4 py-2 rounded-full border focus:outline-none focus:ring-2 focus:ring-purple-500">
            <button  class="bg-purple-600 text-white px-4 py-2 rounded-full hover:bg-purple-700 transition" onclick="sendMessage()">
                Send
            </button>
        </form>
    </div>


<script src="/js/script.js"></script>
</body>
</html>
